experiment:
  system: gfs
  mode: forecast-only

arguments:
  pslot: ${pslot}
  app: ATM
  resdet: 48
  comrot: ${RUNTESTS}/COMROT
  expdir: ${RUNTESTS}/EXPDIR
  idate: 2021032312
  edate: 2021032312
  yaml: ${HOMEgfs}/ci/platforms/gfs_defaults_ci.yaml
