# make for wgrib
CMD = wgrib
$(phony all):	$(CMD)
$(CMD):	wgrib.c
	$(CCMP) -O3 -m64 -o $(CMD) -DDEF_T62_NCEP_TABLE=opn -DFAST_GRIBTAB -DP_TABLE_FIRST wgrib.c
clean:
	-rm -f $(CMD)
install:
	mkdir -p ../../exec
	mv $(CMD) ../../exec

