# This file is used to generate config.emcsfc

config_emcsfc:
  filename: config.emcsfc
  content: !expand |
    #!/bin/ksh -x
    
    # This file is automatically generated from the YAML-based system
    # in ecf/ecfutils/.  Any changes will be overwritten if
    # setup_case.sh is rerun.
    
    ########## config.emcsfc ##########
    echo "BEGIN: config.emcsfc"
    
    export SENDCOM="YES"

    export PDY=`expr $CDATE | cut -c1-8`
    export GDATE=`$NDATE -06 $CDATE`
    export PDY_m6hrs=`expr $GDATE | cut -c1-8`
    export COMINgfs="$DMPDIR/$CDATE/$CDUMP"
    export COMINgfs_m6hrs="$DMPDIR/$GDATE/$CDUMP"
    export COMOUT="$ROTDIR/$CDUMP.$PDY/$cyc"
    
    echo "END: config.emcsfc"
    
