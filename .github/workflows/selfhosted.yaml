# Test Self-hosted-Runner
name: SelfRunner

on: push

defaults:
  run:
    shell: bash

jobs:

  test-self-hosted-runner:

    runs-on: amzonlinx_tmcg
    timeout-minutes: 600

    steps:
      - name: validate-workflow
        run: /home/tmcguinness/temp/hello.sh
