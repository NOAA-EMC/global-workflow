name: Hera 
on:
  pull_request:
    types: [labeled]

jobs:
  test:
    if: ${{ github.event.label.name == 'CI-Hera-Passed' }}
    runs-on: ubuntu-latest

    steps:
    - name: Passed 
      run: echo "CI Passed on NOAA RDHPCS Hera"

  build:  
    if: ${{ github.event.label.name == 'CI-Hera-Building' }}
    runs-on: ubuntu-latest

    steps:
    - name: Building
      uses: schneegans/dynamic-badges-action@v1.6.0
      with:
        auth: ${{ secrets.CLI_DYNAMIC_BADGES}}
        gistID: 9c1670a7eea0bf2a0ef6225af963bb4c
        filename: hera.json
        label: Hera
        message: Building

