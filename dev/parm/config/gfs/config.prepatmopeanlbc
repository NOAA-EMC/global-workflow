#! /usr/bin/env bash

########## config.prepatmiodaobs ##########
# Atm Operational Anl Bias Correction specific

echo "BEGIN: config.prepatmopeanlbc"

# Get task specific resources
. "${EXPDIR}/config.resources" prepatmopeanlbc

echo "END: config.prepatmopeanlbc"
