#! /usr/bin/env bash
# shellcheck disable=SC2034

# Hera-specific job resources

case ${step} in
  "prep")
    # Run on 7 nodes for memory requirement
    tasks_per_node=2
    ;;

  "anal")
    if [[ "${CASE_HIST}" == "C384" ]]; then
      export ntasks=270
      export threads_per_task=8
      export tasks_per_node=$(( max_tasks_per_node / threads_per_task ))
    fi
    ;;

  "anlstat")
    memory="96GB"
    ;;


  "atmanlvar" | "atmensanlobs" | "atmensanlsol")
    export tasks_per_node=12
    ;;

  "aeroanlvar")
    export tasks_per_node=20
    export memory="96GB"
    ;;
  
  "eupd")
    case "${CASE_HIST}" in
      "C1152" | "C768")
        export ntasks=80
        export threads_per_task=20
        ;;
      "C384")
        export ntasks=80
        ;;
      "C192" | "C96" | "C48")
        export threads_per_task=4
        ;;
      *)
        ;;
    esac
    export tasks_per_node=$(( max_tasks_per_node / threads_per_task ))
    ;;

  "ecen")
    if [[ "${CASE_HIST}" == "C768" || "${CASE_HIST}" == "C1152" ]]; then export threads_per_task=6; fi
    export tasks_per_node=$(( max_tasks_per_node / threads_per_task ))
    ;;

  "upp")
    if (( "${CASE_HIST:1}" >= 768 )); then
       # Run fewer tasks per node for memory
       tasks_per_node=20
    fi
    ;;

  "epos")
    tasks_per_node=20
    ;;

  *)
    ;;
esac
