#PBS -S /bin/bash
#PBS -N gfs_atmos_gempak_pgrb2_spec_npoess_0p5_%CYC%
#PBS -j oe
#PBS -q %QUEUE%
#PBS -A %PROJ%-%PROJENVIR%
#PBS -l walltime=03:30:00
#PBS -l select=1:mpiprocs=1:ompthreads=1:ncpus=1:mem=3GB
#PBS -l place=vscatter:shared
#PBS -l debug=true

model=gfs
export cyc="%CYC%"
%include <head.h>
%include <envir-p1.h>

############################################################
# Load modules
############################################################
module load PrgEnv-intel/${PrgEnv_intel_ver}
module load intel/${intel_ver}
module load craype/${craype_ver}
module load cray-mpich/${cray_mpich_ver}
module load cray-pals/${cray_pals_ver}
module load cfp/${cfp_ver}
export USE_CFP=YES
module load wgrib2/${wgrib2_ver}

module list
############################################################
# CALL executable job script here
############################################################
export HOMEglobal=${HOMEgfs}

${HOMEgfs}/jobs/JGFS_ATMOS_GEMPAK_PGRB2_SPEC_NPOESS
if [ $? -ne 0 ]; then
   ecflow_client --msg="***JOB ${ECF_NAME} ERROR RUNNING J-SCRIPT ***"
   ecflow_client --abort
   exit
fi

%include <tail.h>
%manual

TASK gfs_atmos_gempak_pgrb2_spec_npoess_0p5

PURPOSE: Produces NPOESS 0.5-degree GRIB2 subsets and simulated GOES imagery
regridded to 0.25-degree/CONUS grids. Single-instance task looping over
all hours internally.

TROUBLESHOOTING

Long-running (3.5hr wall) single job processing all forecast hours
sequentially. A failure at any internal hour aborts the whole job. Check
the stdout for which hour failed. Input depends on atmos_product output.
If NPOESS is slow, it may hit walltime -- check if the job completed
normally vs PBS killed.

%end
