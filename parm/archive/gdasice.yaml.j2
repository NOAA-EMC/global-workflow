    gdasice:
        {% set head = "gdas.ice.t" + cycle_HH + "z." %}
        name: "GDASICE"
        target: "{{ ATARDIR }}/{{ cycle_YMDH }}/gdasice.tar"
        mandatory:
            # TODO explicitly name the files in history to archive
            - '{{ COM_ICE_HISTORY | relpath(ROTDIR) }}/{{ head }}*'
            # - '{{ COM_ICE_HISTORY | relpath(ROTDIR) }}/ice_in'
