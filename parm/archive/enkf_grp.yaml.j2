enkf_grp:
    name: "ENKF_GRP"
    target: "{{ ATARDIR }}/{{ cycle_YMDH }}/{{ RUN }}_grp{{ ENSGRP }}.tar"
    required:
        {% for mem in range(first_group_mem, last_group_mem + 1) %}
            {% set imem = mem - first_group_mem %}

        # Forecast data
            {% if is_gdas %}
                {% for fhr in range(3, 10, 3) %}
        - "{{ COMIN_ATMOS_HISTORY_MEM_list[imem] }}/{{ head }}atm.f{{ "%03d" % fhr }}.nc"
        - "{{ COMIN_ATMOS_HISTORY_MEM_list[imem] }}/{{ head }}sfc.f{{ "%03d" % fhr }}.nc"
                {% endfor %}
            {% endif %}

        # Store the individual member analysis data
            {% if not lobsdiag_forenkf %}
        - "{{ COMIN_ATMOS_RESTART_MEM_list[imem] }}/{{ head }}gsistat.txt"
            {% endif %}
            {% if DO_JEDIATMENS %}
                {% for iaufhr in IAUFHRS %}
                    {% for itile in range(6) %}
        - "{{ COMIN_ATMOS_ANALYSIS_MEM_list[imem] }}/{{ head }}recentered_jedi_increment.atm.i{{ '%03d' % iaufhr }}.tile{{ itile+1 }}.nc"
                    {% endfor %}
                {% endfor %}
            {% else %}
                {% if do_calc_increment %}
        - "{{ COMIN_ATMOS_ANALYSIS_MEM_list[imem] }}/{{ head }}analysis.atm.a006.nc"
                {% else %}
                    {% for iaufhr in IAUFHRS %}
                        {% set iaufhr = iaufhr %}
        - "{{ COMIN_ATMOS_ANALYSIS_MEM_list[imem] }}/{{ head }}recentered_increment.atm.i{{ '%03d' % iaufhr }}.nc"
                    {% endfor %}  # iaufhr in IAUFHRS
                {% endif %}
            {% endif %}

        # soil DA increments
            {% if is_gdas %}
                {% if DO_GSISOILDA %}
                    {% for iaufhr in IAUFHRS %}
        - "{{ COMIN_ATMOS_ANALYSIS_MEM_list[imem] }}/{{ head }}increment.sfc.i{{ '%03d' % iaufhr }}.nc"
                    {% endfor %}
                {% endif %}
            {% endif %}

        {% endfor %}  # first_group_mem to last_group_mem
