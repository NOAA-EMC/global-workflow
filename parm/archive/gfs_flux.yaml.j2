gfs_flux:
    {% set head = "gfs.t" + cycle_HH + "z." %}
    name: "GFS_FLUX"
    target: "{{ ATARDIR }}/{{ cycle_YMDH }}/gfs_flux.tar"
    required:
        {% for fhr in range(FHMIN_GFS, FHMAX_GFS + FHOUT_GFS, FHOUT_GFS) %}
        - "{{ COMIN_ATMOS_MASTER }}/{{ head }}sflux.f{{ '%03d' % fhr }}.grib2"
        - "{{ COMIN_ATMOS_MASTER }}/{{ head }}sflux.f{{ '%03d' % fhr }}.grib2.idx"
        {% endfor %}
