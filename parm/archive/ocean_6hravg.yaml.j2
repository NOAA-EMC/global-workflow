ocean_6hravg:
    {% set head = "sfs.t" + cycle_HH + "z.6hr_avg" %}
    name: "OCEAN_6HRAVG"
    target: "{{ ATARDIR }}/{{ cycle_YMDH }}/ocean_6hravg.tar"
    required:
        {% for fhr in range(6, FHMAX_GFS + 6, 6) %}
        - "{{ COMIN_OCEAN_NETCDF | relpath(ROTDIR) }}/native/{{ head }}.f{{ '%03d' % fhr }}.nc"
        {% endfor %}
