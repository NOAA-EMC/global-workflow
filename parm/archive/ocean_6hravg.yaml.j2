{% set head = "gfs.ocean.t" + cycle_HH + "z." %}
name: "OCEAN_6HRAVG"
target: "{{ ATARDIR }}/{{ current_cycle | to_YMDH }}/ocean_6hravg.tar"
mandatory:
    {% for fhr in range(FHMIN_GFS, FHMAX_GFS + 6, 6 %}
    - "{{ COM_OCEAN_HISTORY | relpath(ROTDIR) }}/{{ head }}6hr_avg.f{{ '%03d' % fhr }}.nc"
    {% endfor %}
