chem:
    {% set head = CHEM_MODEL | default('gocart') %}
    name: "CHEM"
    target: "{{ ATARDIR }}/{{ cycle_YMDH }}/chem.tar"
    required:
        # TODO explicitize this set
        - "{{ COMIN_CHEM_HISTORY }}/{{ head }}*"
