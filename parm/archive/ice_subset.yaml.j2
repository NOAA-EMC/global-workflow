ice_subset:
    {% set head = "gfs.ice.t" + cycle_HH + "z." %}
    name: "OCEAN_SUBSET"
    target: "{{ ATARDIR }}/{{ cycle_YMDH }}/ice_subset.tar"
    required:
        {% for fhr in range(6, FHMAX_GFS + 6, 6) %}
        - "{{ COMIN_ICE_NATIVE | relpath(ROTDIR) }}/{{ head }}.native.f{{ '%03d' % fhr }}.nc"
        {% endfor %}
