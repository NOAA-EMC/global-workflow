gfswave:
    {% set head = "gfs.t" + cycle_HH + "z." %}

    name: "GFSWAVE"
    target: "{{ ATARDIR }}/{{ cycle_YMDH }}/gfswave.tar"
    required:
        # Wave GRIB2 regional forecast products
        {% set WAVE_OUT_GRIDS_list = WAVE_OUT_GRIDS.split(' ') %}
        {% for grd in WAVE_OUT_GRIDS_list %}
        {% set file_path = ROTDIR ~ '/' ~ RUN ~ '.' ~ cycle_YMD ~ '/' ~ cycle_HH ~ '/products/wave/gridded/' ~ grd %}

        {% for fh in range(0, FHMAX_HF_WAV + FHOUT_HF_WAV, FHOUT_HF_WAV) %}
        # NOTE This is as explicit as possible without major logic to parse wavepostGRD.
        #      Matches files of the form "gfs.tCCz.<region>.<res>.fHHH.grib2".
        - "{{ file_path | relpath(ROTDIR) }}/{{ head }}*.*.f{{ '%03d' % fh }}.grib2"
        - "{{ file_path | relpath(ROTDIR) }}/{{ head }}*.*.f{{ '%03d' % fh }}.grib2.idx"
        {% endfor %}

        # Global wave GRIB2 forecast products
        {% for fh in range(FHMAX_HF_WAV + FHOUT_WAV_GFS, FHMAX_WAV_GFS + FHOUT_WAV_GFS, FHOUT_WAV_GFS) %}
        - "{{ file_path | relpath(ROTDIR) }}/{{ head }}*.*.f{{ '%03d' % fh }}.grib2"
        - "{{ file_path | relpath(ROTDIR) }}/{{ head }}*.*.f{{ '%03d' % fh }}.grib2.idx"
        {% endfor %}

        # Wave point output
        - "{{ COMIN_WAVE_STATION | relpath(ROTDIR) }}/{{ head }}bull.tar"
        - "{{ COMIN_WAVE_STATION | relpath(ROTDIR) }}/{{ head }}cbull.tar"
        - "{{ COMIN_WAVE_STATION | relpath(ROTDIR) }}/{{ head }}spec.tar.gz"

        # Wave boundary point output
        {% if DOBNDPNT_WAVE %}
        - "{{ COMIN_WAVE_STATION | relpath(ROTDIR) }}/{{ head }}ibpbull.tar"
        - "{{ COMIN_WAVE_STATION | relpath(ROTDIR) }}/{{ head }}ibpcbull.tar"
        - "{{ COMIN_WAVE_STATION | relpath(ROTDIR) }}/{{ head }}ibp.tar"
        {% endif %}
        {% endfor %}
